[secrets]
# Signs the session cookie, nothing else. Read from a file because Headplane
# takes `cookie_secret_path`, which keeps it out of the config in the volume.
headplane-cookie-secret = rand-hex 16

[config]
config/config.yaml = %h/.config/containers/volumes/headplane/config/config.yaml
