[config]
# The routes, read at start and never written by the container: the volume is
# mounted read-only, so `caddy reload` comes from here and not from the
# container's own filesystem.
config/Caddyfile = %h/.config/containers/volumes/caddy/config/Caddyfile
