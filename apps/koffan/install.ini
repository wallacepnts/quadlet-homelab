[login]
password = koffan-password

[secrets]
# One password for the whole house: there are no accounts, whoever has it edits
# the list. Generated rather than typed, and it lives in a Podman secret rather
# than in the .env the compose upstream suggests.
koffan-password = rand-base64 18
