# Where the habits are stored. USER_DISK keeps them in the volume as JSON;
# the alternative is a database this deployment does not run.
HABITS_STORAGE=USER_DISK

# Close registration once your account exists, or anyone reaching the address
# can create one.
# MAX_USER_COUNT=1
