# Timezone for the session history.
TZ=America/Sao_Paulo

# The uid/gid the image would drop to. The unit sets User=1000 instead, which
# is what makes the install chown the volumes — these are here only because the
# image reads them, and they must agree with User=.
PUID=1000
PGID=1000
