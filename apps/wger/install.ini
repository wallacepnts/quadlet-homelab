[secrets]
# It signs sessions and cookies — changing it logs everyone out.
wger-secret-key = rand-hex 32

[upstream]
# The GitHub repo per unit, for when the image name does not give it away.
# `-` = no comparable release (an infra base image: it follows the app's own
# compose, rule 9).
wger = wger-project/wger
