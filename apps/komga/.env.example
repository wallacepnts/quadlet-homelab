# The JVM takes what it is given, and the default is a quarter of the host's
# RAM. Scanning a large library is memory-hungry; capping it keeps Komga from
# crowding out the other 70 services.
JAVA_TOOL_OPTIONS=-Xmx1g

# Komga's own settings live in the interface, not here.
