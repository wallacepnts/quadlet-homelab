[config]
# Read at start and never written by the container: the volume is mounted
# read-only, so a change here plus a restart is the whole edit cycle.
config/config.yaml = %h/.config/containers/volumes/headscale/config/config.yaml
