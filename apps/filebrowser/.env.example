# Where the app looks for its config. The image defaults to this path already;
# it is spelled out because a missing config file is a FATAL at startup.
FILEBROWSER_CONFIG=/home/filebrowser/data/config.yaml
