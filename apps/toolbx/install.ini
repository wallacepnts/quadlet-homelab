[upstream]
# The official Toolbx images. None of them come from a GitHub release, so
# `updates.py` has nothing to compare against — follow each distro's own
# release notes instead.
toolbx-arch = -

toolbx-rhel = registry
toolbx-ubuntu = registry
toolbx-fedora = registry
