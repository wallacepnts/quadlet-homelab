TZ=America/Sao_Paulo

# How often the feeds refresh automatically (cron minute format, e.g. 1,31 =
# every 30 min; */20 = every 20 min). Empty = no automatic refresh, manual
# only (Refresh feeds in the UI).
CRON_MIN=1,31
