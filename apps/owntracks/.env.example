TZ=America/Sao_Paulo
OTR_HOST=mosquitto
# The MQTT user — the same one created in Mosquitto's passwd during
# installation, and the same one to configure in the OwnTracks phone app (see
# "Configuring the app" in the README).
OTR_USER=owntracks
