# PUID/PGID of the user running Podman (the same owner as MEDIA_DATA_DIR,
# otherwise the apps cannot write to it) — check with `id -u`/`id -g` and
# adjust the values below before copying.
PUID=1000
PGID=1000
TZ=America/Sao_Paulo
